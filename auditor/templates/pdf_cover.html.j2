<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{ title or "UatuAudit Report" }}</title>
  <link rel="stylesheet" href="../report/print.css">
</head>
<body class="u-cover">
  <header class="u-letterhead">
    <img class="u-logo" src="{{ static_url('uatu-logo.svg') }}" alt="UatuAudit">
    <div class="u-brand">
      <h1>UatuAudit</h1>
      <div>Smart Contract Security</div>
    </div>
    <div class="u-meta">
      <div><strong>Date:</strong> {{ run.generated_at }}</div>
      <div><strong>Contract:</strong> {{ run.contract_id }}</div>
      <div><strong>Repo:</strong> {{ meta.repo or "—" }}</div>
      <div><strong>Ref:</strong> {{ meta.ref or "main" }}</div>
    </div>
  </header>

  <main class="u-cover-main">
    <h2>{{ title or "Security Audit Report" }}</h2>
    <div class="u-badges">
      <span class="badge badge-{{ risk.summary.grade|lower }}">{{ risk.summary.grade }}</span>
      <span class="badge">Score {{ "%.1f"|format(risk.summary.overall) }}</span>
      {% if risk.summary.delta_overall is defined %}<span class="badge">Δ {{ "%.1f"|format(risk.summary.delta_overall) }}</span>{% endif %}
    </div>

    <ul class="u-points">
      <li>Deterministic risk score with baselines & grade buckets</li>
      <li>LLM-assisted assertions with compile precheck (disabled if keys missing)</li>
      <li>STRIDE threats, findings, heatmap, gas analysis, and tests</li>
      <li>Portfolio trend & PR badges/sparklines</li>
    </ul>
  </main>

  <div class="u-watermark">
    <img src="{{ static_url('uatu-mascot.png') }}" alt="">
  </div>

  <footer class="u-footer">© 2025 UatuAudit • report id: {{ run.id }}</footer>
</body>
</html>