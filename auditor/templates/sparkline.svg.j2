<svg xmlns="http://www.w3.org/2000/svg" width="{{ w }}" height="{{ h }}" viewBox="0 0 {{ w }} {{ h }}" role="img" aria-label="risk trend">
  <polyline fill="none" stroke="{{ color }}" stroke-width="2"
    points="{{ points }}"/>
</svg>
